import Link from "next/link";
import SectionHeading from "@/components/SectionHeading";
import { levels, subjects } from "@/lib/data";
export const metadata={title:"Learning Paths"};
export default function Page(){return <main><section className="page-hero"><div className="shell"><span className="breadcrumbs">Home / Learning Paths</span><h1>Your learning should<br/>have a direction.</h1><p>Choose your stage, then your subject. We organise content around clear outcomes rather than leaving you to navigate an endless catalogue alone.</p></div></section><section className="section soft"><div className="shell"><SectionHeading eyebrow="Step 1" title="Choose where you are now."/><div className="level-grid">{levels.map(l=><div className="level-card" id={l.slug} key={l.slug}><small>{l.kicker}</small><h3>{l.name}</h3><p>{l.copy}</p></div>)}</div></div></section><section className="section"><div className="shell"><SectionHeading eyebrow="Step 2" title="Choose what you want to learn."/><div className="subject-grid">{subjects.map(s=><Link className={`subject-card ${s.accent}`} href={`/subjects/${s.slug}/undergraduate`} key={s.slug}><span className="subject-symbol">{s.symbol}</span><h3>{s.name}</h3><p>{s.short}</p><b style={{marginTop:'auto'}}>Build a route →</b></Link>)}</div></div></section></main>}
