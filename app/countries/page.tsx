import Link from "next/link";
import { countries } from "@/lib/data";
export const metadata={title:"Countries & Curricula"};
export default function Page(){return <main><section className="page-hero"><div className="shell"><span className="breadcrumbs">Home / Countries</span><h1>Global subjects.<br/>Local academic context.</h1><p>The core disciplines are universal. School qualifications, terminology and assessment are not. Country routes help learners find material aligned to the system they actually study in.</p></div></section><section className="section soft" style={{paddingTop:30}}><div className="shell course-grid">{countries.map(c=><Link href={`/countries/${c.slug}`} className="course-card" key={c.slug}><span style={{fontSize:36}}>{c.flag}</span><h3>{c.name}</h3><p>{c.copy}</p><div className="topic-cloud" style={{marginTop:'auto'}}>{c.systems.map(x=><span style={{borderColor:'#ddd',color:'#555'}} key={x}>{x}</span>)}</div><b style={{marginTop:20}}>Explore route →</b></Link>)}</div></section></main>}
