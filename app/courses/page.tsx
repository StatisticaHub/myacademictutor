import CourseCard from "@/components/CourseCard";
import { courses, subjects, levels } from "@/lib/data";
export const metadata={title:"Courses"};
export default function Page(){return <main><section className="page-hero"><div className="shell"><span className="breadcrumbs">Home / Courses</span><h1>Courses built to<br/>actually be completed.</h1><p>Focused, structured learning in quantitative and computational subjects, from foundational concepts to postgraduate methods.</p></div></section><section className="section" style={{paddingTop:10}}><div className="shell"><div className="filter-bar">{subjects.map(s=><a href={`#${s.slug}`} className="filter-link" key={s.slug}>{s.name}</a>)}{levels.map(l=><span className="filter-link" key={l.slug}>{l.name}</span>)}</div>{subjects.map(s=><div key={s.slug} id={s.slug} style={{marginBottom:65}}><h2 style={{font:'700 40px Georgia'}}>{s.symbol} {s.name}</h2><div className="course-grid">{courses.filter(c=>c.subject===s.slug).map(c=><CourseCard course={c} key={c.slug}/>)}</div></div>)}</div></section></main>}
