import { notFound } from "next/navigation";
import Link from "next/link";
import { getPathway, pathways } from "@/lib/data";
export function generateStaticParams(){return pathways.map(p=>({slug:p.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPathway(slug);if(!p)notFound();return <main><section className="page-hero"><div className="shell"><span className="breadcrumbs">Pathways / {p.title}</span><span className="eyebrow" style={{display:'block',marginTop:25}}>{p.tag} · {p.duration}</span><h1>{p.title}</h1><p>{p.description}</p><div className="hero-actions"><Link href="/dashboard" className="button">Start this pathway</Link><Link href="/tutoring" className="button button-outline">Plan it with a tutor</Link></div></div></section><section className="section soft"><div className="shell"><span className="eyebrow">Roadmap</span><h2 style={{font:'700 48px Georgia'}}>From foundations to outcome.</h2><div className="module-list">{p.steps.map((s,i)=><div className="module-row" key={s}><span className="module-num">{String(i+1).padStart(2,'0')}</span><div><b>{s}</b><p style={{margin:0,fontSize:13,color:'#706b63'}}>Recommended stage in the {p.title} learning journey.</p></div><span>{i===p.steps.length-1?'Capstone':'Core stage'}</span></div>)}</div></div></section></main>}
